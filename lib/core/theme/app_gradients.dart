import 'package:flutter/material.dart';

class AppGradients {
  static const primary = LinearGradient(
    colors: [Color(0xFF2F6BFF), Color(0xFF00B3FF)],
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
  );
}
